# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# session handoff notes (local only, not for github)
/SESSION_HANDOFF.md

# working docs (local only, not for github)
/docs

# typescript
*.tsbuildinfo
next-env.d.ts

# Castor 파서 산출물 — 커밋하지 않는다 (CI 가 매번 새로 만든다)
graph.json

# 초안 저장소 — 백엔드 테이블이 생기기 전까지만 쓰는 임시 데이터
.draft-data/

# OMC 세션 상태 — 커밋하지 않는다
.omc/
