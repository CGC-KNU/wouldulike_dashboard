# 세틀라이트 — Astro 확장 · Probe · Castor 초안

2026-09-10 · 작성 민열 · 대상 재민(개발) · 민찬(Probe) · 준영/정환(Astro 사용)

이 브랜치(`feat/astro-probe-castor-draft`)는 **화면과 API 경로를 먼저 세운 초안**입니다.
목업이 아니라 실제로 돌아가는 코드이고, 백엔드 테이블만 붙이면 그대로 실데이터로 갈아탑니다.

---

## 0. 한 장 요약

| 툴 | 전 | 후 |
|---|---|---|
| **Astro** | 탭 1개 (식당 관리) | 탭 4개 — 영업 현황 · 식당 관리 · 신규 컨택 · 입금 현황 |
| **Probe** | 타일만 있고 "준비 중" | 탭 2개 — 매장 지표 · **데이터 정합성 점검** |
| **Castor** | 런처에 없음 | 탭 2개 — 화면 지도(코드 자동 파싱) · A/B 후보 |

기존 화면은 **하나도 건드리지 않았습니다.** Papillon·Aether·식당 관리는 그대로입니다.
새 화면은 전부 추가이고, 권한 규칙도 기존 것을 그대로 씁니다.
새 API 는 `src/lib/draft/guard.ts` 가 `/api/dashboard/admin/me/` 로 권한을 확인한 뒤에만 응답합니다 — 미들웨어는 쿠키 존재만 보기 때문입니다.

---

## 1. 돌려보기

```bash
npm install
ASTRO_PREVIEW=1 npm run dev
# 브라우저에서
open http://localhost:3000/auth/preview
```

`ASTRO_PREVIEW=1` 은 **백엔드 계정 없이 화면을 눌러보기 위한 플래그**입니다.
- `/auth/preview` 가 더미 토큰을 심어 미들웨어를 통과시킵니다. 백엔드는 이 토큰을 인증하지 않으므로 실데이터는 한 줄도 안 나옵니다.
- 매장 목록은 `src/lib/draft/previewStores.ts` 의 **2026-09-10 실측 스냅샷 34곳**(id·상호·플랜 그대로)을 씁니다.
- 플래그가 없으면 `/auth/preview` 는 404 이고, `me` 도 예전처럼 백엔드가 판정합니다. **운영에서는 켜지 않습니다.**

백엔드에 붙여서 보려면 `.env.local` 에 `NEXT_PUBLIC_API_URL` 을 넣고 평소처럼 로그인하면 됩니다.

---

## 2. 실데이터와 초안 데이터의 경계

새 화면은 두 종류의 데이터를 씁니다. 섞이는 건 괜찮지만 **어느 쪽인지 모르는 게** 문제라서,
초안이면 화면에 `초안 데이터` 배지가 반드시 붙습니다.

| 데이터 | 지금 어디서 오나 | 배지 |
|---|---|---|
| 매장·플랜·혜택·지표 | 백엔드 실데이터 (`/api/dashboard/*`) | 없음 |
| 입금·계산서·학기/방학·비치물 | 초안 저장소 (`.draft-data/*.json`) | 붙음 |
| 신규 컨택·활동 기록 | 초안 저장소 | 붙음 |
| Castor 그래프 | 코드 파서 산출물 | 붙음 |

초안 저장소는 `src/lib/draft/store.ts` — JSON 파일에 쓰고, 못 쓰면 메모리로 떨어집니다.
**백엔드가 붙으면 통째로 지울 코드입니다.**

### 초안 시드에 지어낸 값이 없습니다

`src/lib/draft/seed.ts` 에 들어간 입금·계산서 상태 14건은 전부 출처가 있습니다 —
09-03 주준영님 보고(입금 5 / 미회신 3 / 발송예정 3), 09-04·09-07·09-09 후속, 09-07 공유된 사업자번호.
출처가 없는 칸은 `null`(모름)로 뒀습니다.

**신규 컨택은 비어 있습니다.** 시트에 24곳이 있다고 들었지만 실물을 못 봐서 채우지 않았습니다.
화면에 시트 열을 그대로 붙여넣는 가져오기가 있습니다 (`매장명 · 단계 · 담당 · 상권 · 메모`, 탭/쉼표 구분).

---

## 3. 백엔드 인계 — 붙이면 자동으로 갈아탑니다

`src/lib/draft/toolProxy.ts` 가 이렇게 동작합니다.

```
프론트 → /api/astro/stores → 백엔드에 붙여본다
                              ├ 200 이면 그대로 쓴다 (draft: false)
                              └ 404/502 면 초안 저장소 (draft: true)
```

그래서 **프론트를 한 줄도 안 고치고** 백엔드만 올리면 됩니다. 확인 방법: 배지가 사라집니다.

Django 앱 초안 3개를 `_backend_changes/` 에 뒀습니다 — `astro/` `probe/` `castor/`.
모델·뷰·URL 이 프론트가 기대하는 응답 형태와 1:1 로 맞춰져 있습니다.
`_backend_changes/README_astro_probe_castor.md` 에 붙이는 순서가 있습니다.

**민찬님께**: `probe/views.py` 의 `_summarize()` 만 실제 쿠폰/스탬프 모델로 채우면 됩니다.
지금은 프론트가 매장마다 `/api/dashboard/stats/` 를 34번 호출하는데, 한 번의 annotate 로 끝날 일입니다.

---

## 4. 설계에서 지킨 것 네 가지

### ① 자동 판정하지 않는다

학기/방학도 입금도 **사람이 확인해서 넣고 툴은 보여주기만** 합니다 (2026-08-12 합의).
자동 추론을 넣으면 틀렸을 때 영업이 그 값을 못 믿게 되고, 결국 다시 시트로 돌아갑니다.

대신 **누가 언제 확인했는지**를 반드시 남깁니다. 지금 시트에 빨간색을 칠하는 방식이 못 하는 일이고,
사실 이것 하나가 Astro 운영 테이블을 만드는 유일한 이유입니다.

### ② KPI 는 총량이 아니라 '지금 막힌 것'

"전체 식당 34" 는 아무 행동도 못 만듭니다. "입금 미확인 6 · 계산서 미회신 2 · 비치물 미전달 7"
은 오늘 할 일이 됩니다. 누르면 목록이 그것만 남습니다. (ADIT Pitchr 대시보드 차용)

입금 화면은 한 단계 더 갑니다 — **우리가 잡고 있는 일**(계산서 미발송)을 맨 위에 따로 셉니다.
ADIT 콘솔에서 "애딧 승인만 3주 넘게 기다린 파트너 3곳"이 나온 이유가 그 숫자가 화면에 없어서였습니다.

### ③ 파이프라인이 계약에서 끝나지 않는다

신규 컨택 단계는 `미컨택 → 컨택 → 미팅조율 → 미팅 → 제안·견적 → 계약 → 입점완료` 입니다.
'계약'과 '입점완료'(첫 콘텐츠·비치물)를 나눠야 영업이 자기가 딴 건이 실제로 굴러갔는지
툴을 갈아타지 않고 봅니다. 7일 이상 안 움직인 카드는 흐려지고 KPI 에 빨갛게 잡힙니다.

### ④ 0 과 '모름'을 섞지 않는다

지표를 못 읽은 매장은 0 이 아니라 회색 `—` 이고 합계에서도 뺍니다.
0 으로 채우면 "이 매장 죽었네" 같은 오판이 납니다.

---

## 4-1. UI 문법 (0910 저녁, 애딧 레퍼런스 반영)

"기존과 달라져도 좋다"는 말에 따라 Astro·Probe·Castor·Aether 를 감싸는 셸과 UI 키트를 새로 짰습니다.
Papillon 은 아윤·재민이 손대고 있는 화면이라 그대로 뒀습니다. **Aether 도 손대지 않았습니다** (개발 총괄이 담당, 민열님 0910 결정). 기존 배너&팝업·마케팅·관리자 설정 탭이 새 셸 안에서 그대로 돌아갈 뿐입니다. 근거는 `00_레퍼런스_네이밍/shots/` 의 Pitchr·Console 실측 48장입니다.

| 가져온 것 | 어디서 | 이렇게 됐다 |
|---|---|---|
| 흰 사이드바 + 아이콘, 활성 항목만 색 | Pitchr 좌측 메뉴 | `_shared/ToolShell.tsx`. 네이비 블록 사이드바는 화면에서 제일 진한 덩어리라 눈이 먼저 거기로 갔다 |
| 본문 13px · 라벨 12px | Pitchr/Console 전반 | 예전 10~11px 마이크로 타이포를 전부 올렸다. 하루 종일 보는 화면이다 |
| 페이지 헤더 = H1 + 오른쪽 주 CTA 하나 | Pitchr `+ 신규 리드` | `PageHeader`. CTA 라벨은 동사+목적어("입금 확인 처리", "후보 추가", "보고 문구 복사") |
| 총량이 아닌 "지금 막힌 것" KPI, 빨강은 0 초과일 때만 | Pitchr `무응답 컨설팅 2건` | `Kpi`. 누르면 표가 그 조건으로 걸러진다 |
| 단계 타일 (→ 로 흐르는 상태) | Console 정산 관리 | `StepTiles`. 입금 현황: 계산서 미발송 → 회신 대기 → 발행·미입금 → 입금 확인 |
| 진짜 `<table>`, 정렬 가능한 머리, 우측 정렬 숫자 | Pitchr 리드 목록 | `Table/Th/Td`. 인라인 펼침 대신 행 클릭 = 오른쪽 패널 |
| 오른쪽 슬라이드 상세 + 상단 단계 표시줄 + 하단 고정 액션 | Console 캠페인 상세 | `SlideOver` + `Stepper`. 목록을 가리지 않는다. 폰에서는 바텀시트 |
| 담당자 필터 알약 (전체/미배정/이름) | Pitchr 파이프라인 | `FilterPills`. "내 카드만"이 한 번의 클릭 |
| 항상 펼쳐진 활동 기록기 (종류 탭 + 본문) | Pitchr 리드 상세 | `ActivityLog`. Enter 로 기록, Shift+Enter 줄바꿈 |
| 제안 → 승인/거부 카드 | Pitchr 에이전트 액션 | 정합성 점검의 각 묶음에 "고치러 가기" 버튼 (해당 툴 탭으로 이동) |
| 스피너 대신 최종 레이아웃 모양 뼈대 | | `Skeleton` |

규칙으로 박은 것: 색은 navy 하나(활성·주 CTA) · 상태는 칩 텍스트가 말하고 점은 실제 조치 필요에만 · 모서리는 버튼 8 / 카드 12 / 칩 pill 셋뿐 · 그림자 대신 1px 선 · 아이콘은 `@tabler/icons-react` 한 벌.

---

## 4-2. 0910 저녁 추가분 (민열님 요청)

| 요청 | 반영 |
|---|---|
| Probe 는 앱 지표도 포괄적으로, 추후 GA 연동 | `probe/AppMetrics.tsx` + `/api/probe/app`. 앱 이용 · 전환 퍼널 · 스탬프/마일리지 · 푸시/배너 17칸에 **출처 칩**(DB·푸시·GA4·Firebase). 값 없는 칸은 빈 칸(0 아님). 출처 연결 순서 DB 집계 → 푸시 열림 → GA4 |
| Astro 는 구글 시트를 많이 참고 | `src/lib/draft/sheet.ts` + `/api/astro/import`. 시트 4탭(현황·신규·후보·계약) 공개 CSV 읽기, **단계 어휘를 시트 그대로**(미컨택 → 컨택 중 → 미팅 조율 → 미팅 예정 → 미팅 완료 → 구두 합의 → 계약 완료 / 재컨택·보류·거절), 열도 시트와 동일. 0910 실측: 후보 61 · 신규 22 · 계약 29 · 현황 32 매칭 |
| 런처 여백 | `_shared/Launcher.tsx`. 툴 아이콘(툴_아이콘 원본 SVG) · 쓰는 사람 · 살아있는 숫자 · 슬랙 채널 링크 · Libra 포함 6장 |
| 툴 페이지 단촐 | 툴마다 **홈** 탭(AstroHome·ProbeHome·CastorHome): 막힌 것 KPI · 파이프라인 막대 · 기한 · 최근 기록 · 시트/슬랙 바로가기 |
| 아이콘 · 슬랙 호환 | `src/lib/satellite.ts` 레지스트리(아이콘·슬랙 채널·시트 ID). 슬랙은 채널 링크(`app_redirect`)까지. 알림·수집 연동은 아직 아님 |
| Aether | 빈 화면 그대로 (개발 총괄 담당) |

HTML 보고서: `Desktop/CGC/04_사내툴_개발/0910_세틀라이트_초안/우주라이크_세틀라이트_초안_0910.html` (스크린샷 7장 포함)

---

## 4-3. 0910 밤 추가분 — 시트를 Astro 로 대체

| 요청 | 반영 |
|---|---|
| 시트에서 하던 수정/추가가 Astro 에서 바로 | 매장 상세·후보 상세에서 **시트 열 전부 편집**(칸에서 나가면 저장). 계약 세부사항 열은 구조화 필드(`coupon_basic`·`stamp_reward`·`pin` …)로 1:1. 후보가 '구두 합의' 이상이면 **제휴 매장으로 등록** 버튼 → 매장 생성(기존 create 경로) + 운영 필드 이관 |
| 계약서·소개서·제안서 바로 다운로드 | **자료실** 탭. 계약서 v6·간소화·부속서식, 제안서 11P·신규매장, 견적서 2종, 안내문, 전단 = 9건 로컬 즉시 다운로드(`public/astro-docs`, gitignore). 팀 소개서·포스터 2건은 드라이브 링크 등록 필요. 영업 단계 순 정렬 |
| 상권별 구분 | 매장 현황·입점 후보에 상권 필터, 홈에 상권별 카드. 시트 현황 탭의 상권 열을 읽음(북문 25 · 정문 1 · 동문 1 · 농장문 1 · 미지정 6) |
| (부수) | 시트와 같은 열 순서 CSV 내보내기(`/api/astro/export?tab=후보\|계약&district=`) — 전환기에 시트로 되돌릴 안전망 |

**UI 정제(민열님 "전체 틀 투박함")**: 채움형 버튼·입력(테두리 없음), 헤어라인 `black/6%`, 눌림 `scale(.97)`, 슬라이드 패널 220ms ease-out 진입 + 스크림, 상단 이중 네이비 바 → 반투명 흰 바(`backdrop-blur`) + 얇은 회색 스트립(iOS 세그먼트), 런처는 큰 타이포·틴트 타일·hover 리프트, "쓰는 사람" 제거. 근거: apple-design(재질·타이포·모션) + emil-design-eng(ease-out·press·transform-only).

---

## 4-4. 세금계산서 탭 (애딧 세발 + Console 볼타 설정)

품의 → 승인 → 발행 상태머신(`PENDING|APPROVED|ISSUING|ISSUED|FAILED|RESULT_UNKNOWN|REJECTED|CANCELED`), 월납 일괄 생성(유료·월납 매장 × 월 이용료, VAT 분리), 발행 주체 설정(볼타 고객 키·공동인증서 만료·승인자·품목 템플릿), 단계마다 슬랙(📄/🧾/✅). 볼타 미연결 → '발행하기' 막힘, '발행 완료로 표시'(승인번호)가 현재 경로. 발행 완료 → 매장 운영 계산서=ISSUED, 입금 처리 → billing=PAID 동기화. `/api/astro/invoices`, `/api/astro/invoices/[id]`(action), `/api/astro/invoices/settings`.

---

## 4-5. 0911 반영분 — 캠퍼스 축 · 월별 입금 · Papillon/Probe 연계 · Probe 운영 화면 · 런처 갤러리

민열님 0911 피드백을 전부 반영했다. 근거는 슬랙 `#sat-probe` `#ops-mileage` `#random`(0906 인사이트 요청) 실측.

**Astro**
- **사람별 구분 제거.** 후보·매장 모두 담당자 필터 없음(멤버가 늘어난다). 담당은 카드 하단 참고 표기만.
- **캠퍼스가 1차 축** — 경북대 · 영남대 · 계명대 세그먼트(`StoreOps.campus`, `Lead.campus`, `CAMPUSES`). 시트 후보 탭(영남대 0909 실측)은 자동으로 영남대, 나머지는 경북대. 상권은 2차 축(알약).
- **칸반 4묶음** — 연락 전 / 연락·미팅 / 합의·계약 / (접힘) 재컨택·보류·거절. 카드 칩은 시트 단계 그대로.
- **매장 추가** — 매장 현황에서 바로. 매장 본체는 `/api/dashboard/admin/restaurants/create`(식당 관리와 같은 경로), 캠퍼스·상권·메모는 Astro 운영 필드에 PATCH. 미리보기에서는 501 안내.
- **입금 현황 = 월별 원장** — 행은 매장 × 월, 실체는 `TaxInvoice`(period). 청구 안 됨 → 품의·승인 → 발행·입금 대기 → 입금 확인. 월 선택, "N월 청구 생성", 행별 입금 확인(발행 전이면 발행 완료 표시를 먼저), 최근 6개월 막대, 보고 문구 복사. 매장 현황의 '9월 입금' 열과 런처 숫자도 **같은 데이터**(월납은 이번 달 계산서, 일시납만 매장 상태값).
- **연계 블록** — 매장 상세 하단 "다른 툴에서 본 이 매장": Papillon(최근 3개월 협찬 촬영 · 콘텐츠 기획, 이름 매칭) + Probe(이번 달 쿠폰·스탬프·재방문·단골). `/api/astro/link?id&name`.

**Probe** (슬랙 #sat-probe · 민찬 태그 건 반영)
- **홍보 인사이트** (`/api/probe/insights`, `StoreInsights.tsx`) — 발행된 Papillon 기획의 topic 에 제휴 매장 이름이 들어가면 자동 포착 → D+2 · D+7 · D+14 체크포인트 → 성과 API 코호트 중앙값 대비 % → **사장님 보고글 초안**(라라더 건 문장 구조: 인사 · 어떤 게시물 · 가장 눈에 띄는 지표 · 해석 · 나머지 한 줄). 담당 태그·채팅방 전달은 사람.
- **마일리지 추첨** (`/api/probe/mileage`, `MileageOps.tsx`) — 수·금 20시 회차표(9월: 1주차 없음, 4주차 추석 몰아 방출 + 2배), 응모풀 확인(미확인 ≠ 0), 결과(예정/추첨 완료/보류/미운용), 규칙 카드(계약 v6 제8~10조 + 0906 결정: 회당 5,000원 1 + 10,000원 1), 금지 표현, 운영 시트·#ops-mileage 링크. 9/2·9/4·9/9 보류를 봇 게시 기준으로 기록. **응모풀 정본 소스 없음**을 화면이 먼저 말한다.
- Probe 홈 상단이 "보고할 차례 · 추첨 응모풀 · 정합성 높음 · 조용한 매장" 으로 바뀜.

**런처 · 셸**
- 갤러리형 카드(앱 아이콘 64px, 살아있는 숫자), **Libra 는 카드가 아니라 하단 스트립**("모든 툴을 슬랙에서 보조합니다", ?현황 ?매장 @Libra).
- **Castor = 보류 · VE 영입 후**(`status: "hold"`, 홈에 안내). 화면은 남김.
- **하단 도크**(`ToolShell` `dock`) — 앱 탭바처럼 툴 전환, '전체'는 런처. 다른 제품 탭으로 뛰는 `go()` 하나로 통일(매장 상세 → Papillon, 정합성 → 매장 현황 등).
- 상단 바 재설계 — `public/brand/wordmark.png`(흰 PNG 라 CSS 마스크로 네이비 채움) · 직무 뱃지 · 사람 알약 안에 로그아웃 아이콘. 제품 내부의 "제품 선택 / 이름" 줄은 Papillon 에만 남김.

**날짜** — `toISOString()` 은 UTC 라 새벽에 어제가 된다. `todayLocal()` `periodLocal()`(ui.tsx)로 통일.

---

## 10. 권장 잠재 반영사항 (내가 권하는 것 — 아직 안 한 것)

| # | 무엇 | 왜 | 어디 |
|---|---|---|---|
| 1 | **Papillon 기획·협찬에 매장 FK** (`ContentPlan.restaurant`, `Sponsorship.restaurant`) | 지금 연계는 이름 매칭. "라라더"와 "라라더 경대점"은 잡히지만 "면 요리 4곳 비교" 같은 제목은 못 잡는다 | 백엔드 · Papillon 에디터에 매장 선택 칸 |
| 2 | **응모풀 자동 적재** (앱 DB → 회차 스냅샷) | 세 번 연속 보류의 원인. 화면·봇·시트 모두 준비돼 있고 이것만 없다 | 재민 · `MileageRound` 모델 |
| 3 | **홍보 인사이트 정오 크론** | 발행 → 매장 매칭 → D+2/7/14 도달 시 세일즈 담당 슬랙 태그. 지금은 화면을 열어야 보인다 | Libra 또는 GitHub Actions |
| 4 | **아윤 지표셋 완성** — 비팔로워 노출 비중 · 성장세(전 7일 대비) | 성과 API 에 `reach_non_followers`, 시계열은 있으나 두 파생값이 없다 | Papillon `performance` |
| 5 | **입금 자동 대조** — 통장 입금 내역 CSV 업로드 → 금액·상호로 계산서 건 자동 매칭 제안(사람 승인) | 매달 13건을 손으로 누른다. 은행 CSV 는 팀이 이미 받는다 | Astro 입금 현황 |
| 6 | **계약 만료 알림** — `contract_ends_on` 30일 전 후보 파이프라인에 '재계약' 카드 자동 생성 | 26-2 계약이 대부분 2월 말에 끝난다. 그때 한꺼번에 오면 늦다 | Astro · Libra |
| 7 | **캠퍼스별 목표 게이지** — 영남대·계명대 앵커 매장(Premium) 선확보 조건을 후보 화면 상단에 | 확장 조건이 문서에만 있다([[woojoolike-expansion-anchor]]) | Astro 입점 후보 |
| 8 | **매장 QR·PIN 검증** — 매장 상세의 PIN 을 앱 쪽 실제 PIN 과 정합성 규칙으로 대조 | 시트 PIN 이 앱과 다른 사고가 이미 한 번 있었다 | Probe 정합성 규칙 추가 |
| 9 | **사장님 보고 발송 이력** — 보고글을 복사한 시점·누가를 기록, 같은 게시물 중복 발송 방지 | 카톡으로 나가는 건 툴이 모른다 | Probe 인사이트 + ActivityLog |
| 10 | **모바일 도크** — 현장에서 폰으로 여는 세일즈를 위해 도크를 하단 고정 탭바로, 사이드바는 접기 | 매장 방문 중 입력이 대부분이다 | ToolShell |
| 11 | **Castor 재개 조건** — VE 합류 시 첫 과제를 "배너 A/B (Q1~Q4 확정안)" 로 고정 | 실험 틀은 이미 있고 사람이 없다 | 채용 후 |
| 12 | **권한 세분화** — Probe 마일리지 결재는 민찬만, 입금 확인은 세일즈 전원 | 지금은 전부 `can_restaurants` 하나 | guard.ts |

## 4-6. 0911 밤 — 애딧 성과 리포트 차용: 리서치 · 에이전트 토론 · 매장 리포트

민열님: "애딧 캠페인 성과 리포트(`console.adit.now/report/<hash>`)를 우리도 차용. 업무툴 리서치 많이, 에이전트 다수(조사/비교/피드백/적용) 토론 후 최종안 적용, 과정·결과·추후 방안·프레임워크를 HTML에."
과정과 원문은 `CGC/04_사내툴_개발/0910_세틀라이트_초안/research/` (브리프 + 1라운드 4편 + 2라운드 4편), 요약은 보고서 HTML "리서치 · 토론 · 리포트" 탭.

**최종안(합의) → 코드**
- `src/lib/draft/report.ts` — 비교 게이트 `comparable()`(n<5 · hidden · 중앙값 0 → 비교 안 함), `approx()`(비교군만 "약"), `interpret()`(점주 문장엔 통계 용어 없이 "평소 N건의 가운데 값"), `buildReportText()`(헤드라인 저장→도달→조회 고정, 다매장 고지, 앱 병렬 서술, 체크포인트별 맺음), `propose()`(P1~P6, 최대 2, 같은 규칙 2회 연속 금지, 표본 게이트 공유), `checkText()`(3층: 단어 · 구조(인과/금액) · PII + 스냅샷에 없는 숫자).
- `src/lib/draft/snapshot.ts` — Papillon 성과·기획 상세(커버·캡션)·앱 지표를 그 순간 값으로 굳힘. `co_stores`.
- `/api/probe/reports` GET/POST, `/api/probe/reports/[id]` PATCH(문구) · POST `approve|link|sent|revoke`. 승인은 서버 게이트, 링크는 미리보기·휘발 저장소(`DRAFT_DATA_DIR` 없음)에서 409/501. **LINKED(발급) ≠ SENT(사람이 보냈음 체크).**
- 공개 페이지 `src/app/r/[token]/page.tsx`(서버 컴포넌트, 스냅샷만, noindex, 확대 허용, OG) + `/api/r/[token]/view` 비콘(클라이언트 1회, 항상 204). 미들웨어는 `^/r/[0-9a-f]{40}$` **정확 매칭**만 공개. 담당자 미리보기는 `/r/preview-<id>`(쿠키 필요).
- Probe 탭 `매장 리포트`(`Reports.tsx`: KPI 검토 대기 · 발급했는데 안 보냄 · 보낸 링크 · 열람됨 + 편집실) · 홍보 인사이트 패널 "리포트 만들기".
- `/api/probe/insights` 재작성(중앙값 라벨, 표본 게이트, "상위권" 제거, 다매장 고지, 앱 블록, 보낸 리포트 표시).

**병합 전 필수 5건(피드백 에이전트) 처리** — `guard.ts actorName()`(서버가 by 찍음) · `isAdminActor()`(계산서 승인/발행 403) · 청구 id 중복 방지(`-2` 접미) · 502/503 폴백 제외 + `DRAFT_FALLBACK=0` · 미리보기 우회는 **미리보기 토큰**(`preview:true`)일 때만. 추가: mark-paid 가 발행 상태를 만들지 않음, 마일리지 회차 현재 월 자동 생성, Astro 홈 입금 기준 통일.

**민열님 결정 필요(토론 미합의)**: ① 큐레이션 게시물에도 리포트를 보낼지(피드백: 단독만 / 적용: 배지+앱 비중↑) ② 카테고리 매장 비교 막대(익명성 때문에 폐기 권고) ③ 월간 리포트를 계약 별지1 이행 내역서로 삼을지(피드백: 묶지 말 것) ④ 링크 만료(없음+자동 회수 vs 계약 종료+90일).

**1차 B(실발송) 선결**: 카카오 개발자 콘솔 도메인 등록 · 영속 저장소(Vercel KV/Postgres 또는 `DRAFT_DATA_DIR` 볼륨, 정답은 Django `StoreReport`) · 커버 `preview_url` 공개 여부 · 위 결정 4개.

## 5. Probe 정합성 점검 — 이번 초안에서 제일 중요한 화면

최근 사고가 전부 같은 모양이었습니다. **데이터 두 곳이 어긋났는데 사람이 우연히 볼 때까지 아무도 몰랐습니다.**

- 09-05 "지금 제휴 아닌 하카타파스타가 튜토리얼에 뜬다"
- 09-10 "첫 가입 화면 쿠폰이 실제 쿠폰과 다르다"
- 09-05 "이런 기본적인 게 아무도 언급 안 됐다는 게 더 문제"

규칙으로 박아두면 화면이 먼저 말합니다. 지금 들어간 규칙 7개:

| 규칙 | 무엇을 비교하나 | 심각도 |
|---|---|---|
| `PAID_NO_BENEFIT` | 유료 플랜 × 활성 혜택 0건 | 높음 |
| `INACTIVE_STILL_EXPOSED` | 비활성 매장 × 활성 기획전 | 높음 |
| `PAID_TIER_UNSETTLED` | 유료 플랜 × 입금 미확인 | 높음/보통 |
| `TIER_MISSING` | 제휴 매장인데 플랜 미지정 | 보통 |
| `INACTIVE_HAS_BENEFIT` | 비활성 매장 × 활성 혜택 | 보통 |
| `OPS_ROW_MISSING` | 유료인데 영업 운영 기록 없음 | 보통 |
| `LEAD_STALE` | 7일+ 무접촉 컨택 | 보통/높음 |
| `PAID_NO_KIT` / `SEASON_UNSET` | 입금 완료 × 비치물 미전달 / 학기·방학 미정 | 낮음 |

각 항목에 **어느 데이터를 비교했는지**와 **어디서 고치는지**를 같이 붙였습니다.
둘 중 하나라도 없으면 "그래서 뭐?" 가 됩니다.

백엔드를 못 읽으면 규칙 대부분이 침묵하므로, 그때는 빨간 경고가 뜹니다 — **'이상 없음'과 구별해야 합니다.**

**다음 단계**: 이 점검을 매일 아침 크론으로 돌려 `높음`이 **새로 생겼을 때만** 슬랙에 올립니다.
매일 다 올리면 아무도 안 봅니다. (`probe/models.py` 의 `QualitySnapshot` 이 그 비교용입니다)

---

## 6. Castor — 코드가 유일한 진실

사람이 화면 목록을 손으로 유지하면 반드시 실제와 어긋납니다. 그래서 파서가 코드에서 뽑습니다.

```bash
node scripts/castor-parse.mjs --post
# CI 라면 배포 워크플로에 이 한 줄
```

의존성 없는 Node 스크립트입니다. `next build` 산출물의 라우트 매니페스트를 먼저 보고,
없으면 `src/app/**/page.tsx` 글롭으로 떨어집니다. `<Link href>` · `router.push/replace` ·
`redirect()` 로 이동을 뽑고, 대문자 컴포넌트 태그를 블록으로 잡습니다.

이 저장소에 돌려본 결과: **화면 17 · 이동 15 · 가드 1 · 못 잡은 이동 0.**
못 잡은 이동 수를 숨기지 않고 화면에 띄웁니다 — 숨기면 사람이 지도를 100% 믿어버리고,
틀렸다는 걸 한참 뒤에 압니다.

파싱 결과(`graphs`)와 사람이 손으로 그은 보정(`overrides`)은 **분리 저장**합니다.
매번 전체를 덮으면 사람 작업이 날아갑니다.

A/B 후보는 네 가지가 다 차야 저장됩니다 — 가설 · 주요 지표 하나 · 가드 지표 · 기간.
하나라도 못 쓰겠으면 아직 실험할 준비가 안 된 겁니다.
출력은 두 벌입니다: `experiment.json`(앱·Probe 가 같은 ID 로 읽음) + 개발 착수 스펙(마크다운).

---

## 7. 정해야 할 것

| # | 내용 | 누가 |
|---|---|---|
| 1 | **신규 컨택 시트 24곳**을 Astro 로 옮길지, 시트를 원본으로 두고 읽기만 할지 | 준영 · 민열 |
| 2 | **입금 확인의 정의** — 통장 입금만인지, 세금계산서 발행까지인지 | 준영 |
| 3 | **학기/방학 값** — 지금은 3단(학기만/방학만/둘 다)입니다. 맞는지 | 준영 |
| 4 | **입금 체크 권한** — 세일즈팀 전원인지 특정인만인지 (지금은 `can_restaurants` 전원) | 민열 |
| 5 | **Probe 를 누가 쓰나** — 지금은 `can_restaurants` 단독(입금 상태를 다루므로). 마케팅에 지표를 열려면 입금 규칙 뺀 별도 탭 필요 | 민찬 |
| 6 | **Castor 대상 리포** — 지금은 이 대시보드 하나입니다. 앱(Flutter)은 어댑터가 따로 필요 | 재민 · 민구 |
| 7 | **변형 노출 방법** — Vercel Edge Config / 미들웨어 분기 중 무엇을 쓸지 | 재민 |
| 8 | 정합성 점검 슬랙 알림을 **어느 채널**로 보낼지 (`#sat-probe` ?) | 민열 |

---

## 8. 이번에 안 한 것

- **매장별 콘텐츠 이력** (8/7 요구 3번). Papillon 발행 데이터와 매장을 잇는 키가 필요해서 미뤘습니다.
  제휴 매장 해시태그로 잇는 안이 09-07 에 나왔는데, 그건 Papillon 쪽 작업이 먼저입니다.
- **시트 양방향 쓰기.** 지금 리브라가 슬랙 보고를 시트에 자동 반영하고 있어 잘 돌아갑니다.
  한꺼번에 옮기려다 둘 다 망가지는 게 흔한 실패라, 읽기만 먼저입니다.
- **AI 제안(승인 큐).** 구조만 지켰습니다 — 제안 → 사람 승인 → 반영. 모델은 나중에.
- **Castor 클릭 가능 미리보기 · Probe 지표 겹쳐보기.** 둘 다 P2 입니다.
- **식당 관리 탭 정리.** 지금 같은 34곳 목록이 '제휴 식당'과 '혜택 조회' 두 번 나옵니다.
  합치는 게 맞지만 재민님 코드라 손대지 않았습니다 — 새 '영업 현황' 화면이 그 형태를 보여줍니다.

---

## 9. 파일 지도

```
src/lib/draft/            초안 계층 — 백엔드 붙으면 지울 것
  types.ts                Astro·Probe·Castor 공용 타입 (Django 모델과 1:1)
  store.ts                JSON 파일 저장소
  toolProxy.ts            "백엔드 먼저, 없으면 초안"
  seed.ts                 출처 있는 시드만
  previewStores.ts        2026-09-10 매장 스냅샷 (ASTRO_PREVIEW 전용)

src/app/api/astro/        stores · stores/[id] · leads · leads/[id] · activities
src/app/api/probe/        overview · quality
src/app/api/castor/       graph(GET/POST) · experiments
src/app/auth/preview/     미리보기 진입 (플래그 없으면 404)

src/app/dashboard/admin/
  _shared/ui.tsx          Button · Input · Kpi · StepTiles · Table · SlideOver · Stepper · FilterPills · Skeleton …
  _shared/ToolShell.tsx   Pitchr 문법의 툴 셸 (Astro·Probe·Castor·Aether)
  astro/                  AstroOverview · StoreDetailPanel · LeadPipeline · BillingBoard · ActivityLog
  probe/                  ProbeOverview · DataQuality
  castor/                 CastorMap · CastorExperiments
  page.tsx                TABS · PRODUCTS 에 새 탭/제품 등록 (그 외 변경 없음)

scripts/castor-parse.mjs  코드 → 화면 그래프 (의존성 없음)
_backend_changes/         Django 앱 초안 astro · probe · castor
```
